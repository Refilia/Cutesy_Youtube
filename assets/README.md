# App assets

Drop a real app icon here as `icon.ico` (256×256 recommended) for a cutesy
branded window/installer icon, then add `"icon": "assets/icon.ico"` under the
`win` block in `package.json`. Until then the default Electron icon is used.
